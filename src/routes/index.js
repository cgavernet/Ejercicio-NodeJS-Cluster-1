const userRouter = require("./user");
const bookRouter = require("./book");
const libraryRouter = require("./library");
const authRouter = require("./auth");

module.exports = {userRouter, bookRouter, libraryRouter, authRouter};
