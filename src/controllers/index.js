const libraryController = require("./libary.controller");
const bookController = require("./book.controller");
const userContoller = require("./user.controller");

module.exports = { libraryController, bookController, userContoller };
